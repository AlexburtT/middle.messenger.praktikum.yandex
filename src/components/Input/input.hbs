<input
		id ="{{ id }}"
		type="{{ type }}"
		name="{{ inputName }}"
		placeholder = "{{ placeholder }}"
		required="{{ required }}"
		autocomplete="{{ autocomplete }}"
		autofocus="{{ autofocus }}"
		minlength="{{ minLength }}"
		maxlength="{{ maxLength }}"
		class="{{ className }}"
/>




